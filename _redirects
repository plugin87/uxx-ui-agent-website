# Cloudflare Workers static assets already normalises HTML URLs: it redirects
# /index.html to / and /about.html to /about on its own. Adding rules that
# point the other way produces a redirect loop, so this file stays empty of
# them and exists only for real redirects added later.
